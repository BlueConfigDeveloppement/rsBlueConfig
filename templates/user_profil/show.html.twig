{% extends 'backoffice.html.twig' %}

{% block title %}UserProfil{% endblock %}

{% block body %}
    <h1>UserProfil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_profil.id }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ user_profil.gender }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ user_profil.lastname }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user_profil.firstname }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ user_profil.phone }}</td>
            </tr>
            <tr>
                <th>Cellphone</th>
                <td>{{ user_profil.cellphone }}</td>
            </tr>
            <tr>
                <th>NaissanceAt</th>
                <td>{{ user_profil.naissanceAt ? user_profil.naissanceAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ user_profil.address }}</td>
            </tr>
            <tr>
                <th>Zipcode</th>
                <td>{{ user_profil.zipcode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ user_profil.city }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ user_profil.avatar }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_profil.createdAt ? user_profil.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ user_profil.updatedAt ? user_profil.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_profil_index') }}">back to list</a>

    <a href="{{ path('user_profil_edit', {'id': user_profil.id}) }}">edit</a>

    {{ include('user_profil/_delete_form.html.twig') }}
{% endblock %}
